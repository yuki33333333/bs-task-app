= form_with model: @task, url: sort_tasks_path, method: :get, local: true do |form|
  = form.select :sort_option, sort_option, selected: params[:sort_option]
  = button_tag type: "submit"
    i.fas.fa-check
